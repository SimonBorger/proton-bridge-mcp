# Top-level conftest. Empty by design -- exists so pytest treats the repo
# root as rootdir and adds it to sys.path, letting tests import the
# top-level proton_bridge_mcp.py module without packaging.
